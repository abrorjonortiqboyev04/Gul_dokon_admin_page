        <div class="card mb-5 mx-5 my-5" style="width: 600px;">
            <img src="{{data.image}}" class="card-img-top" alt="...">
            <div class="card-body">
            <h5 class="card-title">{{data.title}}</h5><hr>
            <h6>Description: </h6>
            <p class="card-text">{{data.description}}</p><hr>
            <p class="card-text">Soni: {{data.mount}} ta</p>
            <p class="card-text">Narxi: {{data.price}} so'm</p>
            <p class="card-text"><small class="text-muted">{{data.createdAt}}</small></p>
            </div>
        </div>

<div class="container">
    <div class="row">
      <div class="col-md-6 offset-3">
        <div class="card mt-3">
          <div class="card-body">
             {{#if error}}
             <div class="alert alert-danger" role="alert">
               {{error}}
             </div>
            {{/if}}
            <form action="/admin/edit/{{data.id}}" method="post">
                
             <div class="mb-3">
                <label for="text" class="form-label">Title</label>
                <input type="text" class="form-control" name="title" id="password" value="{{data.title}}">
              </div>

              <div class="mb-3">
                <label for="text" class="form-label">Product Image</label>
                <input class="form-control" type="text" name="image" id="formFile" value="{{data.image}}">
              </div>

              <div class="mb-3">
                <label for="exampleFormControlTextarea1" class="form-label">Description </label>
                <textarea class="form-control" id="exampleFormControlTextarea1" name="description" rows="4">{{data.description}}</textarea>
              </div>

              <div class="mb-3">
                <label for="number" class="form-label">Amount</label>
                <input type="number" class="form-control" name="mount" id="password" value="{{data.mount}}">
              </div>

              <div class="mb-3">
                <label for="number" class="form-label">Price (so'm)</label>
                <input type="number" class="form-control" name="price" id="password" value="{{data.price}}">
              </div>
               <input type="hidden" name="_csrf" value="{{csrfToken}}">
              <button type="submit" class="btn btn-primary">Save</button>
            </form>
          </div>
        </div>
      </div>
    </div>